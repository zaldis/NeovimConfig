-- configure telescope module here
require('telescope').setup {
    defaults = {
        layout_strategy = 'vertical',
        layout_config = {
          vertical = { width = 0.5 }
        },
    },
}
